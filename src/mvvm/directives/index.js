/**
 * Directives, also known as the `Binder` in `MVVM`,
 * do the Two-Way-binding.
 */

import doubleQuoteVM from './public/text';
import propertyVM from './public/property';
import modelVM from './public/model';

export default {doubleQuoteVM, propertyVM, modelVM};